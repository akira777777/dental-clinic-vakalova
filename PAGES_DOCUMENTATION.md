# Документация по страницам специализаций и врачей

## Обзор

Созданы динамические страницы для всех специализаций (услуг) и врачей клиники. Все кнопки "Подробнее", "Профиль" и "Записаться" теперь функциональны.

---

## 📋 Страницы услуг

### Расположение

`src/app/services/[slug]/page.tsx`

### Доступные услуги

1. **Лечение кариеса** - `/services/lechenie-kariesa`
2. **Профессиональная чистка** - `/services/professionalnaya-chistka`
3. **Отбеливание зубов** - `/services/otbelivanie`
4. **Имплантация** - `/services/implantatsiya`
5. **Протезирование** - `/services/protezirovanie`
6. **Хирургия** - `/services/khirurgiya`
7. **Детская стоматология** - `/services/detskaya-stomatologiya`
8. **Пародонтология** - `/services/parodontologiya`

### Структура страницы услуги

Каждая страница включает:

- **Hero секция** с изображением и названием услуги
- **Ключевая информация**: цена, длительность, категория
- **Преимущества** (5 пунктов)
- **Процесс проведения** (4 этапа)
- **FAQ** (3 часто задаваемых вопроса)
- **Sidebar с CTA**: кнопки "Онлайн-запись" и "Позвонить"
- **Карточка гарантий**
- **SEO оптимизация**: meta-теги, Open Graph, JSON-LD structured data

### Компоненты на странице

```tsx
// Основные кнопки
<Link href="/booking">
  <Button>Онлайн-запись</Button>
</Link>

<a href="tel:+420123456789">
  <Button>Позвонить</Button>
</a>

<Link href="/#services">
  <Button>Назад к услугам</Button>
</Link>
```

---

## 👨‍⚕️ Страницы врачей

### Расположение

`src/app/doctors/[slug]/page.tsx`

### Доступные врачи

1. **Татьяна Вакалова** - `/doctors/tatyana-vakalova`
   - Главный врач, стоматолог-терапевт
   - 15 лет опыта
   - Рейтинг: 4.9 ⭐

2. **Петр Новак** - `/doctors/petr-novak`
   - Стоматолог-хирург, имплантолог
   - 12 лет опыта
   - Рейтинг: 4.8 ⭐

3. **Анна Черна** - `/doctors/anna-cherna`
   - Стоматолог-ортопед
   - 10 лет опыта
   - Рейтинг: 4.9 ⭐

### Структура страницы врача

Каждая страница включает:

- **Hero секция** с фото врача, именем и должностью
- **Рейтинг и отзывы**
- **Языки общения**
- **Кнопки**: "Записаться на прием" и "Позвонить"
- **О враче** - детальное описание
- **Философия работы** - личный подход
- **Образование и сертификаты** (4+ пункта)
- **Специализации** (4 направления)
- **Достижения** (4+ пункта)
- **Sidebar с CTA**: запись и звонок
- **Карточка с отзывами пациентов**
- **SEO оптимизация**: meta-теги, Open Graph, JSON-LD structured data (тип "Physician")

### Компоненты на странице

```tsx
// Основные кнопки
<Link href="/booking">
  <Button>Записаться на прием</Button>
</Link>

<a href="tel:+420123456789">
  <Button>Позвонить</Button>
</a>

<Link href="/#doctors">
  <Button>Назад к специалистам</Button>
</Link>
```

---

## 🔗 Обновленные компоненты

### 1. Services Section (`src/components/sections/services-section.tsx`)

**Изменения:**

- Добавлен `slug` для каждой услуги
- Импортирован `Link` из Next.js
- Кнопка "Подробнее" теперь ведет на страницу услуги:

```tsx
<Link href={`/services/${service.slug}`}>
  <Button variant="outline" size="sm">Подробнее</Button>
</Link>
```

- Кнопки CTA обновлены:
  - "Записаться сейчас" → `/booking`
  - "Связаться с нами" → `/#contact`

### 2. Doctors Section (`src/components/sections/doctors-section.tsx`)

**Изменения:**

- Добавлен `slug` для каждого врача
- Импортирован `Link` из Next.js
- Кнопки обновлены:

```tsx
<Link href={`/doctors/${doctor.slug}`}>
  <Button variant="outline">Профиль</Button>
</Link>

<Link href="/booking">
  <Button>Записаться</Button>
</Link>
```

### 3. Hero Section (`src/components/sections/hero-section.tsx`)

**Изменения:**

- Заменены `<a>` теги на `<Link>` для внутренней навигации
- Кнопка "Записаться на прием" → `/booking`

### 4. Header (`src/components/layout/header.tsx`)

**Изменения:**

- Кнопки "Записаться" используют `Link` вместо `<a>`
- Обе версии (десктоп и мобильная) обновлены

### 5. Footer (`src/components/layout/footer.tsx`)

**Изменения:**

- Ссылки на услуги ведут на конкретные страницы услуг:
  - "Лечение кариеса" → `/services/lechenie-kariesa`
  - "Имплантация" → `/services/implantatsiya`
  - "Протезирование" → `/services/protezirovanie`
  - "Детская стоматология" → `/services/detskaya-stomatologiya`

---

## 🎨 Дизайн и UX

### Общие принципы

1. **Stitch Design System** - современный, чистый дизайн
2. **Адаптивность** - все страницы работают на мобильных устройствах
3. **Доступность** - семантический HTML, ARIA атрибуты
4. **Производительность** - оптимизированные изображения, lazy loading
5. **SEO** - structured data, meta-теги, sitemap

### Цветовая схема

- **Primary**: `#0EA5E9` (Sky Blue) - доверие, чистота
- **Secondary**: `#06B6D4` (Cyan) - медицина, свежесть
- **Accent**: `#10B981` (Emerald) - здоровье, успех
- **Dark mode** поддерживается

### Компоненты UI

- Shadcn/ui components
- Tailwind CSS для стилизации
- Lucide icons для иконок

---

## 🚀 Навигация

### Главная страница (`/`)

**Секции:**

1. Hero - с кнопкой "Записаться на прием" → `/booking`
2. Services - 8 услуг с кнопками "Подробнее" → `/services/[slug]`
3. Gallery
4. Doctors - 3 врача с кнопками "Профиль" → `/doctors/[slug]`
5. Testimonials
6. Contact

### Страница записи (`/booking`)

- Форма онлайн-записи
- Выбор услуги и врача
- Календарь

### Header навигация

```
Услуги → #services (якорная ссылка на главной)
Галерея → #gallery
Врачи → #doctors
Отзывы → #testimonials
Контакты → #contact
Записаться → /booking
```

---

## 📱 Функциональность кнопок

### Типы кнопок

1. **Записаться / Онлайн-запись**
   - Ведут на `/booking`
   - Используют `Link` из Next.js

2. **Позвонить**
   - `<a href="tel:+420123456789">`
   - Открывает телефонное приложение

3. **WhatsApp**
   - `<a href="https://wa.me/420123456789">`
   - Открывает WhatsApp чат

4. **Подробнее / Профиль**
   - Ведут на детальные страницы
   - `/services/[slug]` или `/doctors/[slug]`

---

## 🔧 Техническая реализация

### Static Generation

Все страницы генерируются статически через `generateStaticParams()`:

```tsx
export function generateStaticParams() {
  return Object.keys(servicesData).map((slug) => ({
    slug,
  }));
}
```

### Metadata

Каждая страница имеет динамические meta-теги:

```tsx
export async function generateMetadata({ params }): Promise<Metadata> {
  const { slug } = await params;
  const service = servicesData[slug];

  return {
    title: `${service.title} - Клиника Татьяна Вакалова`,
    description: service.description,
    openGraph: {
      title: service.title,
      description: service.description,
      images: [service.image],
    },
  };
}
```

### JSON-LD Structured Data

**Для услуг:**

```json
{
  "@type": "MedicalProcedure",
  "name": "Лечение кариеса",
  "description": "...",
  "procedureType": "Терапия"
}
```

**Для врачей:**

```json
{
  "@type": "Physician",
  "name": "Татьяна Вакалова",
  "jobTitle": "Главный врач",
  "aggregateRating": {
    "@type": "AggregateRating",
    "ratingValue": 4.9,
    "reviewCount": 156
  }
}
```

---

## ✅ Тестирование

### Проверить функциональность

1. **Главная страница**
   - [ ] Кнопка "Записаться на прием" в Hero → `/booking`
   - [ ] Все 8 кнопок "Подробнее" в Services → `/services/[slug]`
   - [ ] 3 кнопки "Профиль" в Doctors → `/doctors/[slug]`
   - [ ] 3 кнопки "Записаться" в Doctors → `/booking`

2. **Header**
   - [ ] Десктоп: "Записаться" → `/booking`
   - [ ] Мобильный: "Записаться на прием" → `/booking`

3. **Footer**
   - [ ] 4 ссылки на услуги ведут на правильные страницы

4. **Страницы услуг**
   - [ ] "Назад к услугам" → `/#services`
   - [ ] "Онлайн-запись" → `/booking`
   - [ ] "Позвонить" → `tel:+420123456789`

5. **Страницы врачей**
   - [ ] "Назад к специалистам" → `/#doctors`
   - [ ] "Записаться на прием" → `/booking`
   - [ ] "Позвонить" → `tel:+420123456789`

---

## 📊 SEO оптимизация

### Sitemap

Все страницы должны быть добавлены в `sitemap.xml`:

- Главная страница
- 8 страниц услуг
- 3 страницы врачей
- Страница записи

### Robots.txt

```
User-agent: *
Allow: /
Sitemap: https://vakalova-dental.cz/sitemap.xml
```

---

## 🎯 Следующие шаги

1. ✅ Создать все страницы услуг (8/8)
2. ✅ Создать все страницы врачей (3/3)
3. ✅ Обновить все кнопки навигации
4. ✅ Добавить Header и Footer на страницы
5. ⏳ Добавить реальные изображения врачей и услуг
6. ⏳ Настроить форму записи на `/booking`
7. ⏳ Добавить Google Analytics
8. ⏳ Настроить WhatsApp виджет

---

## 📝 Заметки

- Все страницы адаптивны (mobile-first)
- Dark mode поддерживается
- Использованы семантические теги HTML
- SEO оптимизация применена
- Все изображения - placeholder (требуют замены на реальные)
- Телефон +420 123 456 789 - пример (требует замены)

**Дата создания:** 2026-01-20
**Автор:** DimaTorzok (субтитры)
